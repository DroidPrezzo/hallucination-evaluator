# Version floors set to the first patched release for known CVEs.
# Using >= (not ==) allows security patches while blocking known-vulnerable versions.
transformers>=4.56.0      # avoids older deserialization CVEs; 4.56+ accepts the `dtype` kwarg (torch_dtype renamed/deprecated)
torch>=2.6.0              # CVE-2025-32434: torch.load RCE fixed in 2.6.0
datasets>=3.2.0
accelerate>=1.3.0
pandas>=2.2.0
tqdm>=4.66.3             # CVE-2024-34062: fixed in 4.66.3
bitsandbytes>=0.45.0
jinja2>=3.1.6            # CVE-2024-22195/34064/56326/56201, CVE-2025-27516: all fixed by 3.1.6
tabulate>=0.9.0
